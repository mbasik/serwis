{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.profile'|trans }}
{% endblock %}

{% block body %}

    <div class="profile">
        <div class="row">
            <div class="col-lg-12 text-center">
                Nazwa użytkownika:<h2 class="section-heading">{{ user.username }}</h2>
                Adres email:<h3 class="section-subheading text-muted">{{ user.email }}</h3>
            </div>
        </div>
        <div class="row text-center">
          <a href="{{ url('fos_user_change_password') }}" class="page-scroll btn-xl" style="padding: 10px;">{{ 'change.password'|trans }}</a>
      </div>
    </div>
  </section>
    <a href="{{ url('classified_index') }}">Powrót</a>
{% endblock %}
