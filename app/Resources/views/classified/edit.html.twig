{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.classified.edit'|trans }}
{% endblock %}

{% block body %}

                <div class="col-lg-12 text-center">
                    <h2 class="section-heading">{{ 'title.classified.edit'|trans }}</h2>
                </div>

            <div class="row">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="text-center">
                    <input type="submit" class="page-scroll btn-xl" style="padding: 10px;" value="{{ 'submit'|trans }}">
                </div>
                {{ form_end(form) }}
            </div>
    <a href="{{ path('classified_index') }}" class="btn btn-default" title="{{ 'action.view'|trans }}">
        Powrót
    </a>

{% endblock %}